
from bisect import bisect


from bisect import bisect  # Это импортирует МЕДОД СОРТИРВКИ == bisect
for e in range(1, n):
    list.append(fib(e))  
    list1 = list 
print(list)

list1.sort()
i = bisect(list1,0)
list1[i:]+list1[:i]   #тут мы делим как бы индексы до 0 и после 0
list==list1[:n]+list1[n:]  # тут мы получ список сравниваем с тем что получилось ДО


print(list1)
